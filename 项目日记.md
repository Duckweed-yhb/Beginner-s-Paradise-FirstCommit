# 项目日记

> 本文件记录项目的基本信息、里程碑和思路演变。
> （第 1 课创建，持续更新）

---

## 项目基本信息

- **项目名**：FocusStudy 学生专注学习系统
- **要解决的问题**：学生不专注、没计划、不知道时间花在哪
- **目标用户**：初高中生、大学生
- **三大功能**：任务管理 / 番茄专注计时 / 数据统计
- **技术栈**：Vue3 + Element Plus + ECharts（前端）· Python FastAPI（后端）· localStorage + JSON（存储）

## 一句话简介（Elevator Pitch）

> A lightweight student focus study management system with pomodoro timer, task management and learning data visualization to help students improve learning efficiency.

## 参赛信息

- 比赛：Beginner's Paradise — FirstCommit（新手友好黑客松）
- 截止：2026-10-01 05:00（个人截止日：2026-09-28）
- 提交物：可运行项目 / 公开 GitHub 仓库 / 项目描述 / 3-5 分钟 Demo 视频 / README
- 仓库：https://github.com/Duckweed-yhb/Beginner-s-Paradise-FirstCommit

---

## 里程碑

- [x] 第 1 课：完成项目认知与规划
- [x] 第 2 课：开发环境跑通
- [x] 第 3 课：GitHub 仓库建立（2026-09-09 首次推送）
- [x] 第 4-7 课：前端三件套练手完成
- [x] 第 8-12 课：Vue3 前端项目完成（任务 / 计时器 / 统计三页面）
- [x] 第 13-15 课：后端与联调完成（6 个接口 + 代理打通）
- [x] 第 16 课：测试打磨完成（24 个用例通过，修掉 5 个真实 bug）
- [ ] 第 17 课：README 与部署完成 ← **进行中**
- [ ] 第 18 课：视频与提交完成 🎉

---

## 思路演变（重要的设计决定）

### 1. 为什么不做数据库？

一开始想上 SQLite，后来想清楚了：这个比赛评的是"项目能不能跑起来"和"决策是否合理"，不是技术栈有多高深。用 JSON 文件 + localStorage，评委克隆下来两分钟就能跑，不用装任何数据库服务。**对一个单用户的个人工具来说，数据库只会增加门槛，不会带来任何收益。**

### 2. 为什么前端要能脱离后端独立运行？

这是整个项目我最坚持的一个决定。中途我意识到一个问题：如果这个学习工具在"网络不好"或者"后端没开"的时候直接崩掉或者什么都存不了，那它本身就变成了新的干扰源 —— 用户想专注学习，结果先要跟工具打架。

所以数据先写 `localStorage`（本地永远是权威），能连上服务器时再同步过去。**后端是加分项，不是必需项。**

### 3. 为什么把统计逻辑单独抽成 stats.py？

第 16 课做测试时发现：逻辑写在接口函数里，就只能启动整个服务器、发 HTTP 请求来测，又慢又麻烦。把聚合计算抽成纯函数之后，脏数据、空数据、边界值这些情况可以几毫秒验证完。顺手还发现 `bool` 在 Python 里是 `int` 的子类，`True` 会被当成数字 1 算进时长 —— 这个坑不写测试根本发现不了。

### 4. 关于"万物皆插件"的初始设想

我一开始并不想把 FocusStudy 做成"三个功能"，而是想做成一个**平台** —— 每个能力（计时器、任务、图表）
都是一个可以装上、也可以摘掉的插件，中间有一个稳定的内核。

**最终我没有实现插件系统**，做出来的是三个能用的模块。但这个想法实实在在地影响了地基，具体落在三个地方：

| 预留点 | 现在的做法 | 为什么以后好扩展 |
|---|---|---|
| 科目字段 | 任务的 `subject` 是任意字符串，不是写死的学科枚举 | 以后加"编程/考研/乐器"都不用改数据模型 |
| 记录结构 | 一条专注记录 = `subject` + `minutes` + `date` | 任何未来的模块（单词卡、刷题、练琴）都能往同一个结构里写 |
| 接口组织 | API 按资源划分（`/api/tasks`、`/api/records`），统计是独立的 `/api/stats` | 加第四个模块 = 加一组接口 + 一个路由，不需要动核心逻辑 |

这些决定在早期做是**零成本**的，等到项目长起来再改就贵了。
这是这次做项目我觉得挺值的一个判断：**不一定要实现完整的架构，但要让地基不挡住未来的路。**

### 5. 关于"AI 辅助"的边界

比赛规则允许用 AI 辅助，但要求写明用在哪里，而且核心逻辑必须自己理解。我的做法是：**功能设计和数据模型自己想，代码先自己写，写完再让 AI 帮我 review**。这个过程里 AI 帮我找出了时区 bug、局部更新会冲掉字段的问题、以及 Element Plus 的弃用写法 —— 这些我自己没看出来。

具体用了哪些、没用哪些，写在根目录 `README.md` 的 "AI Usage Disclosure" 一节里。

---

## 已知取舍（诚实记录）

- 没有多用户和登录 —— 个人工具，不在本次范围内
- 计时器用 `setInterval`，后台标签页被浏览器限流时会有轻微漂移
- 本地和服务器数据冲突时以本地为准，没有做合并策略
- 前端没有自动化测试，靠第 16 课的手工自测清单

---

## 灵感与备注

- 可以加数据导出，让学生能备份 / 换设备迁移
- 周/月趋势视图比现在的 7 天更有说服力
- 如果时间够，把 ECharts 换成按需引入，现在打的包有点大
