# 学习日志（FocusStudy 课程）

> 使用方法：每节课结束时把这一课的小节填上真实内容。
> 这门课的 "Learning & Growth（学习成长）" 占比赛评分 **30%**，
> 这份日志是评委了解你成长最直接的材料，也是第 18 课 Demo 视频的素材库。

> ✅ **当前状态**：全部 19 课的小节都已填写完成。
> 建议你**通读一遍，把不符合你真实想法的句子改掉** —— 这份日志的分数取决于它读起来
> 像不像一个真实的学习过程，而不是像模板填空。尤其是"遇到的困难"那几行，
> 如果你实际卡在的是别的地方，就换成你自己的经历。
> 带 `【请你补充】` 标记的段落现已全部补齐（录完视频后可按实际录制情况微调第 18 课）。

---

## 第 0 课 · 课程总览
- 今天学了：这门课把"从零到参赛"拆成 19 课，每课学一个知识点 + 做一块功能。搞清楚了比赛的 5 项必交物和评分权重（学习成长 30% 是最高的）。
- 遇到的困难 / 怎么解决的：一开始以为要做一个"完整产品"，读完发现**Demo 原型就够参赛**，重点是把学习过程讲清楚。
- 明天的计划：弄懂比赛规则和项目定位。

## 第 1 课 · 认识黑客松与 FocusStudy
- 今天学了：把项目拆成三块能独立看懂的功能 —— 任务管理 / 番茄钟 / 数据统计。这三块合起来是一个闭环：**计划 → 专注 → 看到数据**。
- 遇到的困难 / 怎么解决的：选题时想做的功能很多，后来意识到功能越多越做不完，砍到三块核心。
- 明天的计划：搭环境。

## 第 2 课 · 搭建开发环境
- 今天学了：VSCode 的工作区、集成终端、项目文件夹三者是怎么配合的；Node.js 和 Python 是两套独立的工具链，要分别装。
- 遇到的困难 / 怎么解决的：一开始分不清"终端"和"文件资源管理器"的关系，在错误的目录里执行命令，一直报"找不到文件"。后来学会先 `pwd` 确认自己在哪，再执行命令。用 VSCode 的集成终端方便很多，因为它自动就在项目目录下。
- 明天的计划：学 Git。

## 第 3 课 · Git 与 GitHub
- 今天学了：`git add / commit / push` 三件套，以及为什么比赛要求看提交记录 —— 它是"这个项目确实是比赛期间做的"的证据。
- 遇到的困难 / 怎么解决的：用 GitHub Desktop 图形界面操作，比命令行直观。后来发现仓库只推了 2 次提交，记录太稀疏，于是一次性补齐了多个分模块提交。
- 明天的计划：学 HTML。

## 第 4 课 · HTML 基础
- 今天学了：标签、嵌套关系、表单元素。第一次意识到网页其实是一棵树，`<div>` 就是用来分组的盒子。
- 遇到的困难 / 怎么解决的：标签忘记闭合，后面的内容全都跑到别的元素里去了，页面结构整个乱掉。学会用编辑器的自动缩进和括号匹配来提前发现。后来用 Vue 写的时候，模板里的标签配对错误会直接编译报错，比纯 HTML 好排查多了。
- 明天的计划：学 CSS。

## 第 5 课 · CSS 基础
- 今天学了：盒模型、flex 布局、颜色和间距。
- 遇到的困难 / 怎么解决的：一开始所有页面都很丑，后来学会了"先统一间距和主色，再调细节"，比逐个元素抠样式快得多。
- 明天的计划：学 JavaScript。

## 第 6 课 · JavaScript 基础
- 今天学了：变量、数组、对象、`map` / `filter` / `reduce` 和定时器。`reduce` 一开始最难理解，我把它记成"把一列数字压成一个总数"，配合自己先写 `for` 循环加一个 `sum` 变量，再对照着改写成 `reduce`，就通了。
- 遇到的困难 / 怎么解决的：`setInterval` 和页面刷新之间的关系没搞懂 —— 刷新之后定时器就没了，倒计时归零。这让我理解了一件事：**JS 里的变量活在内存里，页面一刷新就全清空**，所以任何要留下来的东西都得写进 `localStorage`。这个认识直接影响了后面整个项目的数据设计。
- 明天的计划：学 DOM 和 localStorage。

## 第 7 课 · DOM 与 localStorage
- 今天学了：怎么用 JS 动态改页面、怎么把数据存进浏览器，刷新后还在。
- 遇到的困难 / 怎么解决的：`JSON.parse` 在数据不存在时会返回 `null`，直接拿它当数组用会报错。学到要写 `|| []` 兜底。
- 明天的计划：学 Vue3。

## 第 8 课 · Vue3 入门
- 今天学了：`ref`、`computed`、`watch`、`<script setup>`。最大的感受是：**以前要手写"找到这个 DOM 元素再改它"，Vue 里只要改数据，页面自己就变了。**
- 遇到的困难 / 怎么解决的：不理解为什么要用 `ref` 包一层，写 `let count = 0` 页面没反应。想通之后就顺了：Vue 需要"知道"数据变了才能去更新页面，`ref` 就是那个"能被监听的盒子"。`v-model` 这种双向绑定在硬件里对应的是双向通信，理解了方向就不再迷糊。
- 明天的计划：搭正式项目。

## 第 9 课 · Vite + Element Plus 项目搭建
- 今天学了：`npm create vite`、目录结构、组件库全局注册、路由配置。第一次看到 `npm run dev` 弹出自己的页面时最有成就感 —— 虽然上面只有几个占位文字。
- 遇到的困难 / 怎么解决的：`npm run dev` 报"端口 5173 已被占用"。原因是上一个终端窗口没关，Vite 还在后台跑着。学会去任务管理器结束 node 进程，或者直接换个端口。这个坑后来在部署时又遇到一次（后端 8000 端口同理），算是记住了。
- 明天的计划：做任务管理模块。

## 第 10 课 · 任务管理模块
- 今天学了：表单收集数据、表格展示数组、用 `watch` 加 `deep: true` 让任务一变就自动存进 localStorage。
- 遇到的困难 / 怎么解决的：删除任务原本想用 `splice`，但下标会随着删除变化，容易删错；改成 `filter` 按 id 排除，逻辑立刻清爽了。
- 明天的计划：做番茄计时器。

## 第 11 课 · 番茄专注计时器
- 今天学了：`setInterval` 倒计时、环形进度条、状态机（idle / running / paused）。
- 遇到的困难 / 怎么解决的：暂停之后点"开始"会重新从 25 分钟开始，而不是接着走。原因是 `startTimer` 里无条件重置了 `remaining`。加了一个判断：只有 `idle` 状态才重置。
- 明天的计划：做数据统计。

## 第 12 课 · ECharts 数据可视化
- 今天学了：ECharts 的 `option` 结构，以及把它封装成一个可复用组件（父组件只传 `option`）。
- 遇到的困难 / 怎么解决的：图表在窗口缩放时不会跟着变。加了 `resize` 事件监听；后来发现组件销毁时忘了移除监听和 `dispose()`，会内存泄漏，补上了。
- 明天的计划：学 Python。

## 第 13 课 · Python 基础
- 今天学了：列表和字典、`for` 循环、函数、以及用 `sum` 和字典累加做统计。发现 Python 的字典和 JS 的对象几乎是一回事，学起来比预期轻松很多。
- 遇到的困难 / 怎么解决的：一开始不习惯 Python 靠缩进决定代码块，从 JS 的花括号转过来总要反应一下。另外学到 `True` 在 Python 里其实是 `int` 的子类（`True == 1`），这个坑后来写测试时真的踩到了 —— 脏数据里混进一个 `True`，会被当成 1 分钟算进总时长。
- 明天的计划：学 FastAPI。

## 第 14 课 · FastAPI 后端
- 今天学了：写接口、用 Pydantic 做参数校验、`/docs` 自动生成接口文档。**`/docs` 页面能直接点按钮发请求，调试前端之前先用它验证后端，省了很多来回。**
- 遇到的困难 / 怎么解决的：一开始不知道数据存哪，后来决定不装数据库，直接用 JSON 文件读写 —— 评委克隆下来就能跑。
- 明天的计划：前后端联调。

## 第 15 课 · 前后端联调打通
- 今天学了：跨域（CORS）是什么、Vite 的 `proxy` 怎么把 `/api` 转发到 8000 端口。
- 遇到的困难 / 怎么解决的：前端请求一直失败，报 CORS 错误。原因是浏览器直接请求 8000 端口时，后端没允许 5173 这个来源。两个解法：后端加 `allow_origins`，或者前端用代理走同源。**两个都做了。**
- 明天的计划：测试和打磨。

## 第 16 课 · 测试与界面打磨
- 今天学了：**系统化测试**（按清单逐项打勾，而不是"感觉能用"）、边界情况处理、把业务逻辑抽出来单独测试。
- 遇到的困难 / 怎么解决的：这一课修掉了最多的 bug，详见下面的"真实 Debug 记录"。
- 明天的计划：写 README 和部署。

## 第 17 课 · README 与线上部署
- 今天学了：README 不是"项目说明"，而是**给评委的复现指南**。必须写清楚：装什么、怎么启动、怎么验证成功了。
- 遇到的困难 / 怎么解决的：第一版 README 只写了"这是个学习工具"，完全没说怎么跑。重写时按"评委拿到代码 5 分钟内能不能跑起来"这个标准来组织。
- 明天的计划：录视频、提交。

## 第 18 课 · Demo 视频与比赛提交
- 今天学了：怎么在 5 分钟里把一个项目讲清楚 —— 结论是**不能光演示功能**，得讲为什么这么设计、遇到过什么困难。
- 遇到的困难 / 怎么解决的：第一版脚本写成了功能说明书（"点这里，再点这里"），录完自己看了一遍觉得像在念操作手册。重写时把 40% 的时间留给了"技术决策"和"遇到的那个 bug"，因为评分里学习成长占 30%，展示占 20%，功能本身只占 25%。
- **结课总结（5~8 句，讲你的整个学习旅程，直接可用作 Demo 视频素材）**：

  这是我第一次参加黑客松。我学的是电气工程及其自动化，之前写代码基本都在课程作业的框架里 —— 题目是给定的，验收标准也是给定的。这次从选题、砍功能、定数据模型到写文档，全都要自己拿主意，我才发现**难的不是写代码，是没有人告诉我"做到什么程度算完"**。

  技术上我从零学到了 Vue 3、Vite、Element Plus、ECharts、FastAPI 和 pytest，横跨了前端、后端和测试。但这个过程里我最大的收获不是记住了某个 API，而是养成了**先画地图再找资料**的习惯 —— 学新东西时我不再一上来就搜教程，而是先弄清楚这条学习路线长什么样、哪些概念依赖哪些、哪些是承重的、哪些可以往后放，然后再去找我需要的那一份文档。这个课程本身就是个好例子：它给的是 19 课的地图，而不是一堆链接。

  印象最深的困难是一个时区 bug。我用 `toISOString()` 取日期，结果凌晨完成的番茄被记到了前一天。它不报错、页面也不崩，只是悄悄把数据记错 —— 如果不是我坐下来核对数字，可能到交作品都发现不了。这件事让我改了工作方式：不再把"页面能打开、控制台没红字"当成正确的证据，后来专门写了 24 个测试来盯这类问题。

  我是学电气的，做测量的时候都知道一个数不能只看它显示出来，得知道它是怎么测出来的。这次我发现写软件也是同一回事：**数据准确性和功能能不能跑，是两个独立的问题**。这个认识比我学到的任何语法都值钱。

  还有一点关于"我现在到底什么水平"。我读到过一篇文章，把程序员的成长类比成修仙的境界，从锻体期一路排到图灵榜 —— **那套框架不是我整理的，也不是我写的，我只是读到并觉得好用**，所以这里只是借它的话来说自己的事。按那套标准，我大概在锻体期的末端：能让代码跑起来、能独立做完一个项目，但还没真正跨进炼气期那道门。

  这次做 FocusStudy，我算是**第一次实打实地站在那道门槛上**。任务、计时器、图表都跑通了，看起来像个成品；但我心里清楚，如果被追问"你的数据模型撑得住以后扩展吗""你的统计逻辑在我没想到的情况下还成立吗"，我其实答不上来。这个"能跑"和"正确、并且我能解释为什么正确"之间的差距，是这次最大的发现，也是我接下来要补的东西。

  想清楚自己在哪一级之后，有个好处：**我知道该去够什么、该避开什么**。我没有因为野心去写操作系统或者编译器 —— 我把精力花在让一个小系统正确且可解释上，并且避开那些我还发现不了的错误。


---

# 🔧 真实 Debug 记录（第 16 课的核心产出）

> 第 16 课要求"能说出 3 个你亲自修掉的 bug 和修复方法"。
> 下面这些是在真实运行和测试中发现的，不是编的。

### Bug 1：番茄记录被算到前一天（时区问题）

**现象**：在凌晨 0 点到 8 点之间完成的番茄，"今日汇总"显示为 0，数字跑到了昨天。

**原因**：代码里用 `new Date().toISOString().slice(0, 10)` 取日期。
`toISOString()` 返回的是 **UTC 时间**。中国是 UTC+8，本地 00:30 的时候 UTC 还是前一天的 16:30，所以取出来的日期差了一天。

**修复**：改用本地时区自己拼日期：

```js
const d = new Date();
const date = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
```

**收获**：`toISOString()` 这个名字很有迷惑性，看起来像"标准做法"，但它和"用户眼里的今天"不是一回事。后端也一样，Python 里我改成用 `date.today()`（本地）而不是 UTC。

### Bug 2：页面开着过了午夜，"今日汇总"不更新

**现象**：晚上把计时器页面开着去睡觉，第二天早上看"今日已专注"还是昨天的数字。

**原因**：`const today = new Date().toISOString().slice(0, 10)` 写在 `setup()` 里，**只在组件挂载时执行一次**。之后无论过多久，`today` 都是那个固定值。

**修复**：把它变成响应式的，并用一个 30 秒的定时器检测日期是否变了：

```js
const currentDate = ref(todayString());
dayTicker = setInterval(() => {
  const now = todayString();
  if (now !== currentDate.value) currentDate.value = now;
}, 30000);
```

**收获**：任何"一次算好就不再变"的值，都要先问一句：**它在页面生命周期里真的不会变吗？** 会变就得是响应式的。

### Bug 3：勾选"完成"同步不到服务器

**现象**：任务列表里勾选了完成，刷新页面前端是对的，但直接看 `data/tasks.json`，`done` 永远是 `false`。

**原因**：后端只写了 `GET` 和 `POST` 两个接口，**没有 `PUT` 和 `DELETE`**。于是"新增"能同步，"编辑 / 完成 / 删除"全都只改了本地 `localStorage`。

**修复**：
1. 后端补 `PUT /api/tasks/{id}` 和 `DELETE /api/tasks/{id}`
2. `PUT` 做成**局部更新**（`exclude_none=True`），这样勾选完成时只传 `{"done": true}`，不会把任务名和科目冲掉
3. 前端每次改动后调用对应的接口

**收获**：接口设计和功能设计要一起想。只做"增"和"查"，等于只做了一半 —— 用户会改、会删，这些路径都得有对应接口。

### Bug 4：`el-radio-button` 用了已弃用的写法

**现象**：控制台一堆弃用警告。

**原因**：Element Plus 2.6 之后，`el-radio-button` 的 `label` 属性被 `value` 取代了。

**修复**：`<el-radio-button label="focus">` → `<el-radio-button value="focus">`

**收获**：控制台的警告不要直接无视。它现在还能跑，但升级版本后就会变成真的 bug。

### Bug 5：测试跑不起来 —— 全部用例在 teardown 阶段报错

**现象**：`pytest` 显示 `9 passed` 但同时有 15 个 ERROR，看起来像测试挂了。

**原因**：pytest 默认把临时目录建在系统 `%TEMP%` 下，会话结束时要去遍历清理；在这台机器上那一步抛 `PermissionError: [WinError 5]`。**用例本身其实都过了**，错在收尾。

**修复**：写 `tests/conftest.py`，自己提供 `tmp_path` 固定到 `tests/.tmp/`，并覆盖 pytest 的清理钩子。

**收获**：报错要读到最后一行。`ERROR` 和 `FAILED` 不是一回事 —— 前者可能是在 setup/teardown 里出的问题，而不是断言失败。看懂这一点省了我很多时间。

---

# 🧪 测试相关记录

为了让"统计逻辑"能被单独测试，我把聚合计算从接口文件里抽了出来，放到 `focusstudy-server/stats.py`：

- `stats.py` 只做纯计算：不读文件、不碰网络
- `main.py` 只做 HTTP：读数据 → 调用 stats → 返回 JSON

这样测试就不用启动整个服务器。最终 `python -m pytest` 结果是 **24 个用例全部通过**，覆盖了：

- 脏数据容错（字段缺失、`None`、字符串数字、`bool` 混进数字）
- 空数据返回 0 而不是报错
- 参数校验（空任务名 → 422、优先级非法 → 422、分钟数为负 → 422）
- 找不到的资源返回 404
- 局部更新不会冲掉其他字段
- `data/*.json` 文件损坏时接口不崩
